
# Install some commands
#for command in ngrep
#	do [[ -z `which $command 2>/dev/null` ]] && \
#		sudo install $command
#done

# Update MacPorts
# sudo port -d selfupdate

#bash_completion=/opt/local/etc/profile.d

#if [[ -d ${bash_completion} ]]
#	then
#		for file in ${bash_completion}/{bash_completion.sh,git-completion.bash}
#			do [[ -f ${file} ]] && . ${file}
#		done
#fi

# Try to fix the libintl
# sudo \
#	find /opt/ -name 'libintl*' && \
#	mkdir -p /usr/local/libodep/lib && \
#	ln -s /opt/local/lib/libintl.8.dylib /usr/local/libodep/lib/libintl.8.dylib


